/// This should eventually allow execution from within the client itself.
/// Handled via providing an interface that supports an HTTP or websocket connection and a browser
/// payload. The expection is that as a node in a graph we can support events and blocking communication with
/// source code that runs on the client.