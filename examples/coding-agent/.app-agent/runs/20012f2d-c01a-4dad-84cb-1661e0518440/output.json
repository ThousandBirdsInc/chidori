{
  "result": "Here's a complete report of everything found across all three tasks:\n\n---\n\n## 1. `agent` Function — Symbol Analysis\n\nThe file `examples/coding-agent/agent.star` defines **4 functions** total:\n\n| Symbol | Line | Description |\n|---|---|---|\n| `agent` | 5 | Main entry point — takes `task` and optional `working_dir` |\n| `_get_git_branch` | 35 | Helper — gets current git branch |\n| `_get_timestamp` | 41 | Helper — gets current timestamp |\n| `_get_top_level_listing` | 47 | Helper — lists top-level directory contents |\n\n---\n\n## 2. Lines 5–15 of `agent.star`\n\n```python\ndef agent(task, working_dir = \".\"):\n    # Gather context to inject into the system prompt (MOIM pattern)\n    git_branch = _get_git_branch(working_dir)\n    timestamp = _get_timestamp()\n    top_level = _get_top_level_listing(working_dir)\n\n    system = template(\n        \"prompts/system.jinja\",\n        working_dir = working_dir,\n        git_branch = git_branch,\n        timestamp = timestamp,\n```\n\n**What's happening here:** The `agent` function opens by calling three helper functions to gather live context (git branch, current time, directory listing), then begins rendering a Jinja system prompt template using that context — following the **MOIM (Metadata/Observation Injection Model)** pattern noted in the comment.\n\n---\n\n## 3. All Uses of `'prompt'` in the `coding-agent` Directory\n\n**24 total matches** across 3 meaningful source locations:\n\n### `agent.star` (3 matches)\n| Line | Usage |\n|---|---|\n| 6 | `# Gather context to inject into the system prompt (MOIM pattern)` — comment |\n| 12 | `\"prompts/system.jinja\"` — path to the Jinja system prompt template |\n| 20 | `result = prompt(task, system = system, tools = _TOOLS, max_turns = 50)` — **actual `prompt()` host function call** that sends the task to the LLM |\n\n### `README.md` (2 matches)\n| Line | Usage |\n|---|---|\n| 42 | Describes: *\"A system prompt (Jinja template) is rendered with this context\"* |\n| 50 | Instructs: *\"Edit `prompts/system.jinja` to change the agent's behavior\"* |\n\n### `.app-agent/runs/*/checkpoint.json` (19 matches)\nThese are run-log artifacts from previous executions. They record `\"function\": \"prompt\"` in checkpoint JSON, capturing that the `prompt()` host function was called during those agent runs — this is the framework's replay/checkpointing system at work.\n\n---\n\n### Summary\n\nThe `agent` function uses `prompt` in three distinct ways: as a **comment keyword** describing the MOIM pattern, as a **file path** (`prompts/system.jinja`) for the Jinja template, and most importantly as a **live host function call** — `prompt(task, system=system, tools=_TOOLS, max_turns=50)` — which is the core LLM invocation that drives the entire coding agent.",
  "todos": null
}